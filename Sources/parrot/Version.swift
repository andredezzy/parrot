/// Bumped when a release is tagged. `parrot update` compares this against the
/// newest tag on GitHub, so a stale value here means the daemon believes it is
/// older than it is and offers an update that changes nothing.
let parrotVersion = "0.3.0"
